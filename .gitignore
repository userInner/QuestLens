# dependencies
node_modules/
.pnpm-store/

# build outputs
dist/
build/
out/
cache/
artifacts/
typechain-types/
*.tsbuildinfo

# foundry
forge-cache/
broadcast/

# env files (only the .example versions are tracked)
.env
.env.local
.env.*.local
.env.demo

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# relayer runtime artifacts
.relayer-storage/

# demo bootstrap state (logs, pids, addresses, runtime artifacts)
.demo-state/

# editors
.vscode/
.idea/
*.swp
.DS_Store

# coverage
coverage/
lcov.info

# kiro internal config (user-specific only)
.kiro/specs/*/.config.kiro
