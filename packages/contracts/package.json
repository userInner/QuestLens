{
  "name": "@novaidol/contracts",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv",
    "deploy:local": "forge script script/Deploy.s.sol --rpc-url local --broadcast",
    "deploy:testnet": "forge script script/Deploy.s.sol --rpc-url injective_testnet --broadcast --verify",
    "fmt": "forge fmt",
    "lint": "solhint src/**/*.sol"
  },
  "devDependencies": {
    "solhint": "^4.5.0"
  }
}
