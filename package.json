{
  "name": "novaidol",
  "private": true,
  "version": "0.0.1",
  "description": "AI Virtual Idol with Bonding Curve Tokenomics on Injective",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter frontend dev",
    "test": "pnpm -r test",
    "deploy:local": "pnpm --filter contracts deploy:local",
    "agent:dev": "pnpm --filter agent dev"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
